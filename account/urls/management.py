from django.urls import path
# from account.views import OrganizationDetailView, MemberListView

app_name = 'management' # Namespace isolado para gestão

urlpatterns = [
    # path('settings/', OrganizationDetailView.as_view(), name='org_settings'),
    # path('team/', MemberListView.as_view(), name='team_list'),
]
